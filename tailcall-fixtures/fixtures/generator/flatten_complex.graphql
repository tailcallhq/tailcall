schema {
  query: Query
}
type Query {
  foo: Foo
  bar: Bar
}

# Type with only one field
type Foo {
  bar: Bar
}

# Type with only one field
type Bar {
  baz: Baz
}

# Type with only one field
type Baz {
  a: Int
}
