schema {
  query: Query
}

type Query {
  news: News! @grpc(url: "http://localhost:50051", method: "news.NewsService.GetAllNews")
}

type News {
  id: Int
  title: String
  body: String
  postImage: String
}
