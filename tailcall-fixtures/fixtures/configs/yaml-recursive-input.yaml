server:
  port: 8000
schema:
  query: Query
types:
  - name: Bar
    fields:
      name:
        type:
          name: Foo
      rec:
        type:
          name: Bar

  - name: Query
    fields:
      bars:
        type:
          name: String
        args:
          filter:
            type:
              name: Bar
        graphql:
          args:
          - key: baz
            value: '{{.args.baz}}'
          url: http://localhost
          name: bars
  - name: Foo
    fields:
      name:
        type:
          name: String
