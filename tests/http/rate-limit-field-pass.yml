config: !file tests/http/config/rate-limit-field-pass.graphql
name: Rate Limit Field
mock:
  - request:
      url: http://example.com/bars
    response:
      body:
        - id: 1
        - id: 3
        - id: 5
assert:
  - request:
      method: POST
      url: http://localhost:8080/graphql
      body:
        query: query { bars { id } }
    response:
      body:
        data:
          bars:
            - id: 1
            - id: 3
            - id: 5