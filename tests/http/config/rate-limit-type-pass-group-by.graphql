schema @upstream(baseURL: "http://example.com", batch: {delay: 1, maxSize: 1000}) {
  query: Query
}

type Query {
  bars: [Bar] @http(path: "/bars")
}

type Bar @localRateLimit(requestsPerUnit: 3, unit: "hour", groupBy: "id") {
  id: Int
  name: String
}
