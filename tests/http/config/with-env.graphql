schema @server @upstream(baseURL: "http://jsonplaceholder.typicode.com") {
  query: Query
}

type Query {
  post: Post @http(path: "/posts/{{env.POST_ID}}")
}

type Post {
  id: Int
  title: String
  userId: Int!
  body: String
}
