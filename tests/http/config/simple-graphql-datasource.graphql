schema {
  query: Query
}

type User {
  id: Int
  name: String
}

type Query {
  user(id: Int): User
    @graphQlSource(baseURL: "http://upstream/graphql", query: {name: "user", args: [{key: "id", value: "{{args.id}}"}]})
}
