config: !file tests/http/config/test-expr-concatAll.graphql
name: expr concatAll
assert:
  - request:
      method: POST
      url: http://localhost:8080/graphql
      body:
        query: "query { concatAll }"
    response:
      body:
        data:
          concatAll:
            - 1
            - 2
            - 3
            - 4
