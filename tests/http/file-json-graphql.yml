config: !file tests/http/config/file-json.graphql
name: GraphQL Request from JSON file source
mock:
assert:
  - request:
      method: POST
      url: http://localhost:8080/graphql
      body:
        query: query { users }
    response:
      body:
        data:
          users:
            - id: 1
              name: foo
            - id: 2
              name: bar
