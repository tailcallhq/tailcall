schema
  @server(port: 8803, version: HTTP2, hostname: "localhost")
  @link(src: "example.crt", type: Cert)
  @link(src: "example-nokey.key", type: Key) {
  query: Query
}

type Query {
  greet: String @const(data: "Hello World!")
}
