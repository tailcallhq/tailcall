schema
  @server(port: 8804, version: HTTP2, hostname: "localhost")
  @link(src: "example.crt", type: Cert)
  @link(src: "example-ec.key", type: Key) {
  query: Query
}

type Query {
  greet: String @const(data: "Hello World!")
}
