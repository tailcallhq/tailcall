schema
  @link(src: "server-start-http2-ec.yml", type: Config)
  @link(type: Cert, src: "./example.crt")
  @link(type: Key, src: "./example-ec.key") {
  query: Query
}

type Query {
  greet: String @expr(body: "Hello World!")
}
