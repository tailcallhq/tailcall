schema
  @link(src: "server-start-http2-rsa.yml", type: Config)
  @link(type: Cert, src: "./example.crt")
  @link(type: Key, src: "./example-rsa.key") {
  query: Query
}

type Query {
  greet: String @expr(body: "Hello World!")
}
