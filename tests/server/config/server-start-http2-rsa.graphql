schema
  @server(
    port: 8802
    version: HTTP2
    hostname: "localhost"
    batchRequests: true
  )
  @link(src: "example.crt", type: Cert)
  @link(src: "example-rsa.key", type: Key)
{
  query: Query
}

type Query {
  greet: String @const(data: "Hello World!")
}
