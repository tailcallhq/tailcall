schema
  @server(port: 8801, version: HTTP2, hostname: "localhost")
  @link(src: "example.crt", type: Cert)
  @link(src: "example-pkcs8.key", type: Key) {
  query: Query
}

type Query {
  greet: String @const(data: "Hello World!")
}
