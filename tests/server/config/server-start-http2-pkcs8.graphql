schema
  @link(src: "server-start-http2-pkcs8.yml", type: Config)
  @link(type: Cert, src: "./example.crt")
  @link(type: Key, src: "./example-pkcs8.key") {
  query: Query
}

type Query {
  greet: String @expr(body: "Hello World!")
}
