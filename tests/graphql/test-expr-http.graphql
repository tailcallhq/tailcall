#> server-sdl
schema @server @upstream(baseURL: "http://abc.com") {
  query: Query
}

type Query {
  foo(id: Int!): User @expr(body: {http: {baseURL: "http://jsonplacheholder.typicode.com", path: "/users/{{args.id}}"}})
}

type User {
  id: Int
  name: String
}

#> client-sdl
type Query {
  foo(id: Int!): User
}

type User {
  id: Int
  name: String
}

schema {
  query: Query
}
