#> server-sdl
schema @server @upstream(baseURL: "http://abc.com") {
  query: Query
}

type Query {
  foo: String @expr(body: {const: {data: "hello from server"}})
}

#> client-sdl
type Query {
  foo: String
}

schema {
  query: Query
}
