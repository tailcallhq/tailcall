#> server-sdl
schema @server(auth: [{JWT: {jwks: {file: "tests/server/config/jwks.json"}}, id: "jwt"}]) @upstream {
  query: Query
}

type Query {
  data: String @const(data: "data") @protected
}

#> client-sdl
type Query {
  data: String
}

schema {
  query: Query
}
