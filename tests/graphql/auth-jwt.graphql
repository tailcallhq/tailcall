#> server-sdl
schema @server @upstream @auth(JWT: {jwks: {file: "tests/server/config/jwks.json"}}) {
  query: Query
}

type Query {
  data: String @const(data: "data") @protected
}

#> client-sdl
type Query {
  data: String
}

schema {
  query: Query
}
