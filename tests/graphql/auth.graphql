#> server-sdl
schema @server(auth: [{basic: {file: "tests/server/config/htpasswd.txt"}, id: "basic"}, {id: "jwt", jwt: {jwks: {file: "tests/server/config/jwks.json"}}}]) @upstream {
  query: Query
}

type Query {
  data: String @const(data: "data") @protected
}

#> client-sdl
type Query {
  data: String
}

schema {
  query: Query
}
