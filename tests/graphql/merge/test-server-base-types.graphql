#> server-sdl
schema @server(baseURL: "http://abc.com", port: 3000) {
  query: Query
}

type Query {
  hello: String @const(data: "world")
}

#> server-sdl
schema @server(baseURL: "http://xyz.com", upstream: {proxy: {url: "http://localhost:3000"}}) {
  query: Query
}

type Query {
  hello: String @const(data: "world")
}

#> merged-sdl
schema @server(baseURL: "http://xyz.com", port: 3000, upstream: {proxy: {url: "http://localhost:3000"}}) {
  query: Query
}

type Query {
  hello: String
}
