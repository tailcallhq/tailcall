#> server-sdl
schema {
    query: Query
    enableGraphiql: "/graphql"
}

type Query {
    hello: String
}

#> client-sdl
type Failure
@error(message: "Cannot use restricted route '/graphql' for IDE", trace: ["schema", "@server", "enableGraphiql"])
