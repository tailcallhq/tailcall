#> server-sdl
schema {
  query: Query
}

type User {
  id: ID!
}

type Query {
  user(id: ID): User @http(path: "/user/{{args.id}}", baseURL: "http://localhost:8080")
}

#> client-sdl
type Failure @error(message: "argument 'id' is a nullable type", trace: ["Query", "user"])
