#> server-sdl
schema {
  query: Query
}

type Query {
  user: User # This is a root-level configuration without a resolver
}

type User {
  id: ID
  name: String
}

#> client-sdl
type Failure
@error(message: "No root-level resolver found in the schema.", trace: ["Query", "user"])
