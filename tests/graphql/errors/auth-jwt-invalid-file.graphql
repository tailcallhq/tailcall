#> server-sdl
schema @server(auth: [{id: "jwt", JWT: {jwks: {file: "__unknown.json"}}}]) {
  query: Query
}

type Query {
  data: String @const(data: "data")
}

#> client-sdl
type Failure
  @error(
    message: "No such file or directory (os error 2)"
    trace: ["schema", "@server", "auth", "JWT", "jwks", "file", "__unknown.json"]
  )
