#> server-sdl
schema {
  query: Query
}

type User {
  name: String
}

type Query {
  user: User @file
}

#> client-sdl
type Failure
  @error(
    message: "@file must have parameter src"
    trace: ["Query", "user", "@file"]
  )
