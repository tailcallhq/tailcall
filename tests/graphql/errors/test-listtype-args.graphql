#> server-sdl
schema {
  query: Query
}

type User {
  id: ID!
}

type Query {
  user(id: [ID]): User @http(path: "/user/{{args.id}}", baseURL: "http://localhost:8080")
}

#> client-sdl
type Failure @error(message: "can't use list type 'id' here", trace: ["Query", "user"])
