#> server-sdl
schema {
  query: Query
}

type Query {
  data: String @const(data: "data") @protected
}

#> client-sdl
type Failure
  @error(
    message: "@protected operator is used without defining auth on schema's @server"
    trace: ["Query", "data", "@protected"]
  )
