#> server-sdl
schema @server @upstream @link(src: "tests/graphql/test-graphql-with-link.graphql", type: Config) {
  query: Query
}

#> client-sdl
enum Foo {
  BAR
  BAZ
}

type Post {
  id: Int!
  user: User
}

type Query {
  foo: Foo
  post(id: Int!): Post
}

type User {
  id: Int
  name: String
}

schema {
  query: Query
}
