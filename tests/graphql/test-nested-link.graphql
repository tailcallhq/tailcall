#> server-sdl
schema @server @upstream @link(src: "../fixtures/graphql-with-link.graphql", type: Config) {
  query: Query
}

#> client-sdl
enum Foo {
  BAR
  BAZ
}

type Post {
  id: Int!
  user: User
  userId: Int!
}

type Query {
  foo: Foo
  post(id: Int!): Post
}

type User {
  id: Int
  name: String
}

schema {
  query: Query
}
