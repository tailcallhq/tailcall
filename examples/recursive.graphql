schema @server(graphiql: true) @upstream(baseURL: "https://jsonplaceholder.typicode.com") {
  query: Query
}

type User {
  name: String
  id: Int
  friend: User @http(path: "/users/2")
}

type Query {
  user: User @http(path: "/users/1")
}
