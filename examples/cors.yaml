# yaml-language-server: $schema=../generated/.tailcallrc.schema.json

server:
  port: 8000
  hostname: "0.0.0.0"
  headers:
    cors:
      allowOrigins:
        - "*"
      allowHeaders:
        - "*"
      allowMethods:
        - POST
        - GET
        - OPTIONS

links:
  - src: ./jsonplaceholder.graphql
