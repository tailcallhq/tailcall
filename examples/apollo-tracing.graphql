schema {
  query: Query
}

type Query @cache(maxAge: 30000) {
  posts: [Post] @http(url: "http://jsonplaceholder.typicode.com/posts")
}

type Post {
  id: Int!
  userId: Int!
  title: String!
  body: String!
}
