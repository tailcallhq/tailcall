schema {
  query: Query
}

interface IUser {
  id: ID!
  name: String!
}

type Post {
  id: ID!
  title: String!
}

type Query {
  user: [UserQuery]
}

type UserQuery implements IUser {
  id: ID!
  name: String!
  posts: [Post]
}
