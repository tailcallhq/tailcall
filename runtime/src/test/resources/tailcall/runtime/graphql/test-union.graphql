#> server-sdl
schema {
  query: Query
}

scalar Bar

scalar Foo

union FooBar = Foo | Bar

type Query {
  foo: FooBar
}

#> client-sdl
schema {
  query: Query
}

scalar Bar

scalar Foo

union FooBar = Foo | Bar

type Query {
  foo: FooBar
}
