#> server-sdl
schema {
  query: Query
}

type Query {
  foo: Foo @http(path: "/defaultPropertyResolver", baseURL: "https://foo.com")
}

type Foo {
  a: Int
}

#> client-query
query @expect(output: {foo:{a:1}}) {
  foo {
    a
  }
}
