#> server-sdl
schema {
  query: Query
}

type Query {
  foo: String @modify(name: "bar") @http(path: "/renameField", baseURL: "https://foo.com")
}

#> client-query
query @expect(output: {bar:"Hello World"}){
  bar
}
