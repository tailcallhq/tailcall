#> server-sdl
schema {
  query: Query
}

type Query {
  identity: String @http(path: "/resolveWithHeaders", query: {data: "{{headers.authorization}}"}  baseURL: "https://foo.com")
}

#> client-query
query @expect(output: """{"identity":"bar"}""") {
  identity
}

