#> server-sdl
schema {
  query: Query
}

type Query {
  foo(input: Int @modify(name: "data")): Bar
}

type Bar {
  bar: Int @http(path: "/renameArgument", baseURL: "https://foo.com")
}

#> client-query
query @expect(output: """{"foo":{"bar":1}}""") {
  foo(data: 1) {
    bar
  }
}
