#> server-sdl
schema {
  query: Query
}

type Query {
  user: User @http(path: "/resolvedByParent", baseURL: "https://foo.com")
}

type User {
  address: Address @inline(path: ["street"])
}

type Address {
  street: String
}

#> client-query
query @expect(output: """{"user":{"address":"James Street"}}"""){
  user {
    address
  }
}

