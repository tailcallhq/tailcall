#> server-sdl
schema {
  query: Query
}

type Query {
  foo: Foo @http(path: "/listFields", baseURL: "https://foo.com")
}

type Foo {
  a: Int
	b: [Int]
}

#> client-query
query @expect(output: {foo:{a:1,b:null}}) {
  foo {
    a
		b
  }
}
