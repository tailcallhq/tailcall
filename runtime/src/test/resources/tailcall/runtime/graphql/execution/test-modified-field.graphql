#> server-sdl
schema {
  query: Query
}

type Identity {
  a: Int @modify(name: "b")
}

type Query {
  identity: Identity @http(path: "/modifiedField",baseURL: "https://foo.com")
}

#> client-query
query @expect(output: {identity:{b:1}}) {
  identity {
    b
  }
}
