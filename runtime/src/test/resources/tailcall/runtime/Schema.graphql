type User {
  id: ID!
  name: String!
  email: String!
  username: String!
  phone: String
  website: String
  posts: [Post]
}

type Post {
  id: ID!
  title: String!
  body: String!
  user: User
}

type Query {
  users: [User]
  posts: [Post]
}

schema {
  query: Query
}
