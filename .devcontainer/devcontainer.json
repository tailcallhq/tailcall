{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
    "ghcr.io/devcontainers/features/rust:1": {
      "version": "1.78",
      "profile": "default"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y linux-tools-common linux-tools-generic linux-cloud-tools-generic nginx wrk && sudo apt-get install -y linux-tools-$(uname -r) || echo 'linux-tools for specific kernel not installed' && cargo install flamegraph"
}
