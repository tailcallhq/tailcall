{
  "name": "tailcall",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "npx wrangler deploy --minify",
    "test-debug": "npx wrangler dev --port 19194 --remote --env debug",
    "test-release": "npx wrangler dev --port 19194 --remote"
  },
  "devDependencies": {
    "wrangler": "^3.22.2"
  }
}
