{
  "name": "tailcall",
  "scripts": {
    "lint": "prettier --check .",
    "lint:format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.29.0",
    "lint-staged": "^13.1.0",
    "prettier": "2.8.0"
  },
  "lint-staged": {
    "**/*": [
      "prettier --write --ignore-unknown"
    ]
  }
}
