{
  "name": "tailcall-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "publish": "wrangler publish",
    "dev": "wrangler dev --local"
  },
  "devDependencies": {
    "wrangler": "^2.13.0"
  }
}
