#> server-sdl
schema {
  query: Query
}

input Foo {
  bar: String
}

type Query {
  foo(input: Foo): String @modify(name: "data")
}

#> client-sdl
schema {
  query: Query
}

input Foo {
  bar: String
}

type Query {
  data(input: Foo): String
}
