#> server-sdl
schema @server(baseURL: "http://localhost:4000") {
  query: Query
}

type Query {
  foo: String @http(path: "/foo",headers: {foo: "bar"})
}

#> client-sdl
schema {
  query: Query
}

type Query {
  foo: String
}
